# name: Frontend CI

# on:
#   push:
#     branches: [ "main", "develop" ]
#   pull_request:
#     branches: [ "main", "develop" ]

# jobs:
#   frontend-ci:
#     runs-on: ubuntu-latest

#     services:
#       mysql:
#         image: mysql:8.0
#         env:
#           MYSQL_ROOT_PASSWORD: root
#           MYSQL_DATABASE: car
#           MYSQL_USER: user
#           MYSQL_PASSWORD: password
#         ports:
#           - 3306:3306
#         options: >-
#           --health-cmd="mysqladmin ping -h localhost"
#           --health-interval=10s
#           --health-timeout=5s
#           --health-retries=5

#     steps:
#       # 1. Checkout del repo
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       # 2. Configurar Node
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: 'npm'
#           cache-dependency-path: frontend/package-lock.json

#       # 3. Instalar dependencias
#       - name: Install Dependencies
#         working-directory: ./frontend
#         run: npm install

#       # 4. Esperar a MySQL
#       - name: Wait for MySQL
#         run: |
#           echo "Esperando a MySQL..."
#           sudo apt-get install -y mysql-client
#           for i in {1..15}; do
#             if mysql -h 127.0.0.1 -u user -ppassword -e "SELECT 1" car; then
#               echo "MySQL está listo!"
#               break
#             fi
#             echo "MySQL no responde todavía..."
#             sleep 5
#           done

#       # 5. Lint
#       - name: Run ESLint
#         working-directory: ./frontend
#         run: npm run lint --if-present

#       # 6. Tests
#       - name: Run Tests
#         working-directory: ./frontend
#         run: npm test --if-present

#       # 7. Build
#       - name: Build Frontend
#         working-directory: ./frontend
#         run: npm run build
